{
  "name": "synapse",
  "version": "0.1.0",
  "description": "Where Claude's thought becomes Codex's code",
  "type": "module",
  "main": "src/index.ts",
  "bin": {
    "synapse": "src/index.ts"
  },
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch src/index.ts",
    "test": "bun test",
    "typecheck": "bunx tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
